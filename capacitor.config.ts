import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'budget-tracking-standalone',
  webDir: 'www'
};

export default config;
